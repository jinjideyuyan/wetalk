# 				   正则表达式学习笔记

正则表达式能够得到众多语言和工具的支持是有原因的:它们极其有用。从较低的层 面上来说，正则表达式描述的是一串文本(a chunk of text)的特征。读者可以用它来验证 用户输入的数据，或者也可以用它来检索大量的文本。从较高的层面上来说，正则表达式 容许用户掌控他们自己的数据——控制这些数据，让它们为自己服务。掌握正则表达式， 就是掌握自己的数据。



完整的正则表达式由两种字符构成。特殊字符(special characters，例如文件名例子 中的*)称为“元字符”(metacharacters)，其他为“文字”(literal)，或者是普通文本字 符(normal text characters)。



文本检索是正则表达式最简单的应用之一——许多文本编辑器和文字处理软件都提供 了正则表达式检索的功能。最简单的就是egrep。在指定了正则表达式和需要检索的文件 之后，egrep会尝试用正则表达式来匹配每个文件的每一行，并显示能够匹配的行。



*** 云字符的定义

## 元字符

1. 行的起始和结束
2. 字符组 (或的关系)





## 示例

****** 环视功能

## ***正则表达式修饰符 - 可选标志



正则表达式是从左到右逐一字符进行匹配！！！

匹配分两种：匹配内容 / 匹配位置

匹配位置：检查某个位置，是否符合要求

顺序环视的意义：检查某个位置，但检查时匹配的字符串并不在（最终）“匹配的字符串”内



增强的行锚

点”(enhanced line anchor)匹配模式



```shell
增强的行锚
点”(enhanced line anchor)匹配模式
```



## 历史流派

## 匹配原理

## 常用的正则例子





Perl特殊换行指定